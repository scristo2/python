import winshell
    import sys
    import os      

    try:

      aa = winshell.move_file(

        source_path=desktop(),  ##el archivo que quieres  cambiar
        target_path=os.path.join(desktop(), 'eso', 'errores.log'), ##destino
      )


    

    except winshell.x_winshell as err:

        print('no se h apodido')   
