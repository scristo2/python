https://riptutorial.com/tkinter/example/27784/scrolling-a-canvas-widget-horizontally-and-vertically
 canvas = tk.Canvas(self)
        scroll = Scrollbar(self, command=canvas.yview)
        frame = tk.Frame(canvas)
        for i in range(100):

            button = tk.Button(frame, text='holl amundo cjcdjd')
            button.configure(width=200, anchor='w')
            button.pack()
        canvas.create_window(0,0, anchor=tk.NW, window=frame)
        canvas.update_idletasks()

        canvas.configure(scrollregion=canvas.bbox('all'), yscrollcommand=scroll.set)
        frame.configure(bg='red')
        canvas.pack(side=tk.LEFT, fill=tk.BOTH, expand='yes')
        scroll.pack(side=tk.RIGHT, fill=tk.Y)
